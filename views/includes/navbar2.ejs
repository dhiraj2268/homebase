<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Centered Navbar with Icons</title>
    <!-- Bootstrap CSS -->
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <!-- Font Awesome for Icons -->
    <link
      href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css"
      rel="stylesheet"
    />
    <link rel="shortcut icon" href="/favicon.svg" type="image/svg+xml" />
    <script src="https://unpkg.com/ionicons@5.5.2/dist/ionicons.js"></script>
    <link rel="stylesheet" href="/css/rating.css">
    <style>
      /* Custom styles */
      .navbar {
        background-color: #001f3f; /* Navy Blue */
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      }
      .navbar-brand,
      .nav-link {
        color: #ffffff !important;
        font-weight: 600;
        transition: color 0.3s ease;
      }
      .nav-link:hover {
        color: #f39c12 !important;
        text-shadow: 0 0 10px rgba(243, 156, 18, 0.7);
      }
      .form-control {
        border-radius: 30px;
        box-shadow: inset 0 0 5px rgba(0, 0, 0, 0.1);
      }
      .btn-search,
      .btn-profile,
      .btn-cart {
        border-radius: 30px;
        transition: background-color 0.3s ease, transform 0.3s ease;
      }
      .btn-search {
        background-color: #ffffff;
        color: #001f3f;
        border: none;
      }
      .btn-search:hover {
        background-color: #f2f2f2;
        transform: scale(1.05);
      }
      .btn-profile {
        background-color: #f39c12;
        color: #ffffff;
        border: none;
      }
      .btn-profile:hover {
        background-color: #e67e22;
        transform: scale(1.05);
      }
      .btn-cart {
        background-color: #ffffff;
        color: #001f3f;
        border: none;
      }
      .btn-cart:hover {
        background-color: #f2f2f2;
        transform: scale(1.05);
      }
      .navbar-nav {
        margin: 0 auto; /* Center the navbar items */
      }
    </style>
  </head>
  <body>
    <nav class="navbar navbar-expand-lg">
      <div class="container-fluid">
        <a class="navbar-brand" href="#">MyWebsite</a>
        <button
          class="navbar-toggler"
          type="button"
          data-bs-toggle="collapse"
          data-bs-target="#navbarNav"
          aria-controls="navbarNav"
          aria-expanded="false"
          aria-label="Toggle navigation"
        >
          <span class="navbar-toggler-icon"></span>
        </button>
        <div
          class="collapse navbar-collapse justify-content-center"
          id="navbarNav"
        >
          <ul class="navbar-nav">
            <!-- Centered Items -->
            <li class="nav-item">
              <a class="nav-link active" aria-current="page" href="/properties/propertyList">Home</a>
            </li>
            <!-- <li class="nav-item">
              <a class="nav-link" href="#">About</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Service</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="#">Property</a>
            </li> -->
            <li class="nav-item">
              <a class="nav-link" href="/contact">Contact</a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/properties/propertyList/new">Add Property</a>
            </li>
          </ul>
        </div>
        <form class="d-flex me-3">
          <input
            class="form-control me-2"
            type="search"
            placeholder="Search"
            aria-label="Search"
          />
          <button class="btn btn-search" type="submit">
            <i class="fas fa-search"></i>
          </button>
        </form>
        <div class="d-flex">
          <a href="/seller" class="btn btn-profile me-2"
            ><i class="fas fa-user"></i
          ></a>
          <a href="#" class="btn btn-cart"
            ><i class="fas fa-shopping-cart"></i
          ></a>
        </div>
      </div>
    </nav>

    <!-- Bootstrap JS (Optional) -->
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.11.7/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.min.js"></script>
  </body>
</html>
